<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="XamarinApp.Views.NewTaskPage"
             Shell.PresentationMode="ModalAnimated"
             Title="New Task"
             xmlns:ios="clr-namespace:Xamarin.Forms.PlatformConfiguration.iOSSpecific;assembly=Xamarin.Forms.Core"
             ios:Page.UseSafeArea="true">
    <ContentPage.Content>
        <StackLayout Spacing="3" Padding="15" BackgroundColor="White">
            <Entry Text="{Binding Text, Mode=TwoWay}" FontSize="Medium" />

            <StackLayout Orientation="Horizontal" >
                <CheckBox IsChecked="{Binding IsChecked}" />
                <Label Text="Priority"  VerticalTextAlignment="Center"/>
            </StackLayout>

            <StackLayout Orientation="Horizontal">
                <Label Text="Complete Until:" VerticalTextAlignment="Center" />
                <DatePicker Date="{Binding CompleteUntil, Mode=TwoWay}" />
            </StackLayout>

            <StackLayout Orientation="Horizontal">
                <Button Text="Cancel" Command="{Binding CancelCommand}" HorizontalOptions="FillAndExpand"></Button>
                <Button Text="Save" Clicked="OnSaveClicked" HorizontalOptions="FillAndExpand"></Button>
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>

</ContentPage>
